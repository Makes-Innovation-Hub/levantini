import * as S from "./CategoryStatus.styles";

const LockMessage = () => {
  return (
    <S.LockMessage>Complete the previous category to unlock this one!</S.LockMessage>
  );
};

export default LockMessage;
